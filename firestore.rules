rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access is through the authenticated server, never the browser SDK.
    match /{document=**} { allow read, write: if false; }
  }
}
